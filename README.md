# expressApi
This is my template for a basic restful api using express.js. It updates, deletes, adds, and creates a movie item for a movie array of movie objects. Has some basic validation of properties, and if the item exists, otherwise sends status error as a response. 
